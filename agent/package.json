{
  "name": "@zerowatch/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "simulate": "node --loader ts-node/esm src/agent.ts",
    "server": "node --loader ts-node/esm src/server.ts",
    "operator": "node --loader ts-node/esm src/operator.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "rm -rf dist && tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "^0.16.0",
    "@midnight-ntwrk/midnight-js-network-id": "^4.0.4",
    "express": "^4.19.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
